# 1.21.x 多版本适配落地方案

> **[已冻结 2026-09-08]** 本方案已执行完毕（P0~P5 全部闭环：1.21.2~1.21.8 离线构建全绿 + 实机四缺陷修复验证）。
> `1.21.x` 分支进入维护态；1.21.9+ 适配请见新分支方案：[1.21.9-1.21.11-porting-plan.md](1.21.9-1.21.11-porting-plan.md)。
> 新分支已将 v1_21_6 组终态吸收回主源集并删除全部旧覆盖组目录，本文档中的覆盖组目录结构仅在该 git 历史中存在。

> 分支：`1.21.x`（自 `1.21.1` 基线创建）
> 目标：在单分支上以「版本矩阵 + 兼容组覆盖源集」方式适配 Minecraft 1.21.2 ~ 1.21.8，复用 1.20.x 分支已验证的多版本构建模式。
> 本文档为落地施工蓝图，版本号带「待校准」标记的项在对应阶段开工前需按附录 A 渠道核对。

---

## 1. 背景与目标

- 现状：`1.21.1` 分支（v1.4.1）为单版本发布（Fabric + Forge + NeoForge），工具链为 Gradle 8.11.1 + architectury-loom 1.13.467 + mojmap + Java 21。
- 目标：1.21.2、1.21.3、1.21.4、1.21.5、1.21.6、1.21.7、1.21.8 逐版本产出发布 jar；1.21.1 保持现状不动。

已定案决策（用户确认）：

- **Forge 冻结在 1.21.1**：Forge 官方链在 1.21.2+ 仅社区维护、占有率极低，且本项目 forge 模块多处 API 已处于移除流程（`DistExecutor`、`IExtensionPoint.DisplayTest` 等）。1.21.2+ 由 Fabric + NeoForge 覆盖；forge 模块代码保留在分支上随 G1 维护，矩阵中 1.21.2+ 标记 `forge_supported=false` 不参与构建。
- **1.21.9 ~ 1.21.11 不纳入本轮**：NeoForge 21.9 起 FML 启动/类加载重构、vanilla 渲染分相重构，断层过大；待本方案落地后另行立项。

## 2. 现状盘点

### 2.1 源码风险面

模块结构：`common`（96 文件，直接引用 mojmap `net.minecraft.*`）+ `fabric`（10）+ `forge`（9）+ `neoforge`（10）。common 通过自建桥接层（`DataPathProvider`、`ClientBridge`、`ClientPlatformAdapter`）与 loader 解耦，非 Architectury 注解方案。

关键利好：

- 无任何 Mixin（`todolist.mixins.json` 的 mixins 数组为空）、accesswidener 无条目、不注册任何游戏内容（零 `Registry.register`/`DeferredRegister`）。
- H2 存储主体为纯 JDBC，仅 4 个文件触及 `CompoundTag`/`NbtIo`/`Component`。
- 自定义列表组件（`TaskListWidget`/`ProjectListWidget`/`ScrollBar`）为手动驱动自绘，不继承 vanilla widget，规避了 `AbstractSelectionList` 系列签名风险。

版本敏感点（按风险排序）：

| # | 风险点 | 位置 | 影响版本 | 等级 |
|---|--------|------|----------|------|
| 1 | `GuiGraphics.drawString/drawCenteredString` 坐标 int→float、重载调整 | common 13 文件约 31 处（`TaskListWidget`、`TodoHudRenderer`、`TodoScreenRenderSupport` 等） | 1.21.2 起 | 高（量大，编译期机械修复） |
| 2 | `Screen.render` / `Entry.render` 渲染链与签名（renderWidget 拆分、mouse 坐标 int→double） | 8 个 Screen 子类 + `ProjectSettingsScreen` Entry | 1.21.2、1.21.6 | 高 |
| 3 | Fabric `HudRenderCallback` 移除 → `HudLayerRegistrationCallback` | fabric `TodoClient.java:126` | 1.21.2 起 | 高（HUD 唯一挂接点） |
| 4 | NeoForge 21.2 入口改造（构造器注入 mod bus/ModContainer、registerExtensionPoint 迁移；双总线划分不变）、21.6 GUI 层重构（`RenderGuiEvent.Post`） | neoforge `NeoForgeTodoClient`、`NeoForgeNetworkBridge` | 1.21.2、1.21.6 | 中高 |
| 5 | mojmap 包结构大规模重组 → import 机械修复波及全量 | common/fabric/neoforge 全部 | 1.21.5 | 中高（量大面广） |
| 6 | 渲染管线重构：`RenderSystem.enableBlend/defaultBlendFunc/setShaderColor` | common `TodoHudRenderer.java:908-910`（全项目唯一底层渲染调用） | 1.21.6 | 中 |
| 7 | `KeyMapping` 系统重构（category 类型变化） | 三平台客户端类各 3 个键位注册 | 1.21.6 | 中 |
| 8 | `EditBox`/`AbstractSliderButton` 构造与 hint 机制 | 7 个 Screen 约 13 实例 | 1.21.2+ 各处 | 中 |
| 9 | 网络 payload API（`PayloadTypeRegistry`/`PayloadRegistrar`/`StreamCodec`） | common `TaskPackets`/`ProjectPackets` + 三平台 payload | 各版本小改 | 中低 |
| 10 | 离线自测 fake 体系反射字段名与构造签名（见 2.2） | common test 源集（`GuiTestSupport`、`FakeMinecraftClient` 等） | 各组 | 中（显式熔断，必须随组维护） |

### 2.2 测试现状（决定分层验证策略）

- 离线自测体系：12 个 JavaExec 任务挂在 `:common:check`，约 350+ 手写断言用例。**逻辑层保护厚实**：命令/权限矩阵（~130 用例）、加入审批链（~35）、主界面回归（113）、任务列表交互（~20，含拖拽排序全家桶）、HUD 数据层（25）、持久化安全网（~10）、H2 全链路（含 13 个真实 socket 级 TCP 用例）。
- 离线测试**只能保证逻辑层**，以下五类只能实机暴露：
  1. 真实渲染视觉（颜色混合、scissor 裁剪、省略号截断、面板重叠）
  2. 真实输入事件流（`charTyped` 字符管线、修饰键、真实右键 button 1、滚轮精度、拖拽阈值）
  3. HUD 真实分层（与原版 HUD/boss bar/聊天的遮挡、guiScale≠1 缩放、透明度混合）
  4. 真实存档/网络生命周期（世界进出、LAN 真实网络栈、断线重连、tick 驱动）
  5. 真机性能（H2 保存卡顿、帧率、线程竞争）
- 两个「离线全绿、实机异常」的静默失真源：`FakeFont` 恒 6px/字符（真实中文约 9-12px，布局断言与真实渲染可能偏离）；`FakeMinecraftClient.execute()` 同步执行（掩盖异步时序）。版本迁移后新增第三类：fake 体系以「编译失败/`NoSuchFieldException` 启动崩」的形式**显式熔断**——这是预期信号，必须随版本组修复 fake，而非绕过。
- 平台层（入口类、键位注册、HUD 挂接、事件总线、modmenu 集成）在离线测试中被 stub，是每个新版本实机冒烟的必查项。

## 3. 适配范围与版本分组

按 API 断层划分兼容组（组内共用一份覆盖源码）：

| 组 | MC 版本 | 断层性质 | 说明 |
|----|---------|----------|------|
| G1 | 1.21.1 | 基线 | 现有 `src/main/java` 即 G1 实现，不动 |
| G2 | 1.21.2 / 1.21.3 | GUI 文本坐标 float 化、HUD 回调参数、NeoForge 入口构造注入 | 1.21.3 为 1.21.2 的兼容性后续 |
| G3 | 1.21.4 | item model/资源侧重，GUI 影响小 | 预期改动最少 |
| G4 | 1.21.5 | mojmap 包重组、数据组件与渲染 API 调整 | import 波及面最大 |
| G5 | 1.21.6 / 1.21.7 / 1.21.8 | 渲染管线重构、KeyMapping 重构、GUI 层重构 | 1.21.7/1.21.8 为 hotfix 级 |

逐版本独立编译、独立发布 jar（沿用 1.20.x「一版本一 jar」惯例）。

Loader 覆盖：G1 = Fabric + Forge + NeoForge；G2~G5 = Fabric + NeoForge（Forge 冻结，见第 1 节决策）。

## 4. 总体技术方案

### 4.1 单分支 + 版本矩阵（沿用 1.20.x 模式）

与 1.20.x 分支的 `gradle/version-matrix.properties` + `build-local-120x.bat` 完全同构：

- 新增 `gradle/version-matrix.properties`（1.21.x 格式，见附录 A 初稿）。
- 新增 `build-local-121x.bat <目标MC版本> [Gradle args...]`：读取矩阵 → 校验 `build_supported` → 设置 `JAVA_HOME=D:\JAVA\JDK\jdk-21.0.2`（全系统一 Java 21，无需 JDK 切换）→ 以 `-Ptarget_*` 注入 gradle → 构建后拷贝产物到 `dist\`。
- 工具链全矩阵统一：Gradle 8.11.1 + architectury-loom 1.13.467 + mojmap。loom 对 MC 版本向下兼容，无需像 1.20.x 那样校验 loom/gradle 版本组合（矩阵中保留字段以便未来升级）。若高版本组编译遇到 loom 兼容问题，仅升级矩阵该组 `loom_version` 并同步 `settings.gradle.kts` 读取方式（当前已按 property 读取，支持注入）。

### 4.2 兼容组覆盖源集机制（核心新增）

1.21.x 各组间存在编译期不兼容的签名变化（如 `drawString(int...)` 在 1.21.2 后消失），无法用单一源码编译全部版本。机制如下：

- 矩阵每版本增加 `api_group` 字段（`v1_21_1`/`v1_21_2`/`v1_21_4`/`v1_21_5`/`v1_21_6`），bat 注入 `-Ptarget_api_group=<组>`。
- 模块源码目录约定（common、fabric、neoforge 同构；forge 仅基线）：
  - `src/main/java`：G1 基线实现（现内容，永不改写为「最新版 API」）。
  - `src/main/v1_21_2/java`、`v1_21_4/java`、`v1_21_5/java`、`v1_21_6/java`：该组与基线的差异类。**同包同名类整体覆盖**基线版本；新增类直接放入。
  - test 源集同样支持 `src/test/v1_21_x/java`（fake 体系的版本组归宿，见 2.2）。
- Gradle 接线参考实现（各 loom 子项目 build.gradle.kts）：

```kotlin
// 读取矩阵注入的兼容组；缺省为基线组，保证 IDE/旧脚本零迁移
val apiGroup = (findProperty("target_api_group") as String?) ?: "v1_21_1"

if (apiGroup != "v1_21_1") {
    val overrideDir = layout.projectDirectory.dir("src/main/$apiGroup/java")
    // 收集覆盖目录中的相对路径，从基线源集排除同名文件，避免 duplicate class
    val overriddenPaths = overrideDir.asFileTree.files.map {
        it.relativeTo(overrideDir).path
    }
    sourceSets.named("main") {
        java.setSrcDirs(listOf(
            fileTree("src/main/java") { exclude(overriddenPaths) },
            overrideDir
        ))
    }
    // test 源集同法处理 src/test/$apiGroup/java（存在时）
}
```

- 版本声明注入：`fabric.mod.json` / `mods.toml` / `neoforge.mods.toml` 中的硬编码版本（`"minecraft": "1.21.1"`、`[52,)`、`[21.1,)` 等）改为占位符，由 `tasks.processResources` 的 `expand` 注入矩阵值（新增 `-Ptarget_minecraft_version_range`、`-Ptarget_loader_range` 等属性，命名与 1.20.x 矩阵对齐）。
- 根 `build.gradle.kts`：`distReleaseJars`/`h2JarContentCheck` 按 `forge_supported` 跳过 forge（1.20.x 分支已有同款逻辑可移植：`findProperty("target_forge_supported")`）。

## 5. 方案边界

以下边界为施工红线，逐项明确：

1. **基线不可变**：`src/main/java`、`src/test/java`（G1/1.21.1 实现）在任何阶段不得改写为高版本 API；高版本差异一律写入对应覆盖目录。G1 的 bug 修复走基线 + 同步评估各组覆盖目录是否需要回移。
2. **覆盖粒度**：覆盖目录只放「与基线有差异的类」与「高版本新增类」。资源文件（lang/icon/pack.mcmeta/mixins.json/accesswidener）全版本共用，不参与覆盖机制；若未来某版本资源格式变化，再单独扩展该边界。
3. **组内编译假设**：组内所有版本共用该组最高版本的覆盖目录编译。前提假设：1.21.3 对 1.21.2、1.21.7/1.21.8 对 1.21.6 为二进制兼容级差异。**拆组触发条件**：组内低版本编译失败且无法用反射/条件代码兜底时，为低版本增设子覆盖目录（如 `v1_21_6_min/`）或拆为独立组。拆组决策必须记录到本文档第 3 节。
4. **协议边界**：不做跨 Minecraft 版本的网络协议兼容——服务器与客户端同版本联机（MC 本身协议即版本锁定）。自定义包协议版本号保持 1，跨版本互通超出本方案范围。
5. **发布边界**：一版本一 jar，`fabric.mod.json`/`neoforge.mods.toml` 的 minecraft 依赖精确锁定为 `[1.21.x]`，不使用跨组版本区间（与 1.20.x 惯例一致，避免「1.21.1 jar 装到 1.21.5」的运行期风险）。
6. **fake 维护边界**：版本组迁移导致 fake（`GuiTestSupport` 反射字段名、`Fake*` 系列构造签名）熔断时，修复方式同样是「覆盖目录替换」，禁止在基线 test 源集上直接改高版本 API；禁止通过删除断言/放宽断言让测试变绿（AGENTS.md：修实现不修测试，除非测试断言本身错误，此时须在提交说明中写明依据）。
7. **Forge 边界**：forge 模块仅在 G1 矩阵路径参与构建；G2+ 构建不编译 forge 模块，`distReleaseJars`/`h2JarContentCheck` 自动跳过（附录 B 第 5 项）。forge 源码不删除、不随高版本组维护，留待未来单独评估。
8. **测试边界**：离线自测保证逻辑层；渲染视觉、真实输入流、HUD 分层、真实生命周期、真机性能五类以实机回归清单兜底（第 6 章）。两者不可互相替代。
9. **工具链边界**：全矩阵统一 Java 21 / Gradle 8.11.1 / loom 1.13.467。任何一组的工具链升级（如未来某组必须升 loom）视为矩阵字段变更，须全矩阵回归受影响组，不允许「某组悄悄换工具链」。
10. **提交边界**：每个版本（或每组基建改造）独立提交序列；禁止跨阶段混合提交；覆盖目录新增文件同样遵守类/方法注释要求。

## 6. 分层测试体系

目标：每个发布 jar 在六个层次上有明确定义的验证动作，重点封堵「离线全绿、实机基础功能异常」。

### 6.1 层次定义与执行矩阵

| 层 | 内容 | 触发时机 |
|----|------|----------|
| L1 编译矩阵 | 全模块 `compileJava` + `compileTestJava`（fake 熔断在此暴露） | **每个版本** |
| L2 离线自测 | `:common:check` 12 项 JavaExec 全绿（约 350 用例逻辑回归） | **每个版本** |
| L3 产物校验 | jar 产物 depends 与矩阵一致、`h2JarContentCheck` 通过、`tripletSampleCheck` 通过 | **每个版本** |
| L4 实机单机回归 | 见 6.2 清单 | **组首版本全量**（1.21.2/1.21.4/1.21.5/1.21.6）；组内后续版本（1.21.3/1.21.7/1.21.8）跑 6.2 中标 ★ 的精简集 |
| L5 实机联机回归 | 见 6.3 清单（LAN 发布 + 第二客户端） | **组首版本**；组内后续版本仅做「双端增删改同步」单项 |
| L6 性能双路径 | H2 卡顿与保存路径按 AGENTS.md 规则 15/16 双路径验证 | **组首版本**（G4、G5 必做；G2/G3 若存储层无改动可引用基线结论） |

### 6.2 L4 实机单机回归清单

按「版本迁移最易破坏」排序，★ 为组内后续版本也要跑的精简项：

**平台层（离线测试完全 stub，每次必查）**
- ★ mod 正常加载无报错日志；ModMenu 能打开且入口正常、配置界面（ConfigScreen）可打开
- ★ K/H/J 三键位生效；原版「按键设置」中键位分类与名称显示正常、无冲突标记

**主界面与渲染视觉（对冲 drawString float 化、字体度量失真）**
- ★ K 打开主界面：布局无错位、无文本截断/重叠；中文（zh_cn）显示正常（重点：CJK 真实宽度下的行省略、面包屑、详情面板长文本裁剪）
- ★ 四档响应式布局：窗口拉宽/缩窄时三列/内联/紧凑覆盖层切换正常
- 面板重叠遮罩、滚动条边缘、优先级色块、标签前置视觉正常

**真实输入流（离线无 GLFW 事件）**
- ★ EditBox：点击聚焦、光标移动、中文 IME 输入、回车确认、ESC 取消、点击外部失焦自动保存（详情面板）
- ★ 真实滚轮滚动任务列表与滚动条；真实拖拽排序（含拖到边缘自动滚动）
- 真实右键（button 1）打开上下文菜单并执行；搜索框过滤
- 子任务展开/折叠点击、复选框点击热区准确

**HUD（对冲 HUD API 更换与渲染管线重构）**
- ★ HUD 默认渲染；H 键切换展开、J 键切换可见性
- 与原版 HUD（聊天、boss bar、F3）遮挡关系正常；透明度混合正常（重点 1.21.6 组）
- guiScale=2/3 下布局不错位；ConfigScreen 拖拽 HUD 预览位置后保存重启仍生效

**数据与持久化**
- ★ 新增/编辑/勾选完成/删除任务、优先级升降档、项目新建/切换/星标，操作即时生效
- ★ 退出世界重进 + 完全退出游戏重启，任务与项目数据保留
- `/todo` 基础命令（help/add/list/done）可用；配置文件损坏回落默认

**G5 专项（1.21.6/7/8）**
- 移除 `RenderSystem` 调用后 HUD 透明度/混合与基线一致
- KeyMapping 重构后键位分类归属正确

### 6.3 L5 实机联机回归清单（LAN 发布）

- 发布本地世界（TCP 激活路径）：无报错，外部客户端可加入
- 团队项目：创建、成员添加、加入审批流（申请→通知→批准/拒绝）
- 双端同步：A 端新增/勾选/重排任务，B 端打开中的 GUI 实时刷新；陈旧编辑合并保留服务器侧变更
- 权限：普通成员只读、管理员可管理、view-only 拒绝编辑
- 服务器侧保存后：客户端断线重连、退出重进，数据一致（AGENTS.md 规则 16 链路：**团队保存后重新打开待办，再切回个人项目**，必测）
- H2 双路径（AGENTS.md 规则 15）：未发布/嵌入式 与 已发布/TCP 两条路径下的保存流畅度目测一致，无明显卡顿

### 6.4 实机 bug 预防对照表（断层 × 盲区交叉）

| API 断层 | 最可能的实机症状 | 对应检查项 |
|----------|------------------|------------|
| 1.21.2 `drawString` float 化 | 文本半像素抖动、截断位置变化 | 6.2 主界面与渲染视觉 |
| 1.21.2 HUD API 更换（分层化） | HUD 层级错乱：被原版 HUD 遮挡或遮挡原版 | 6.2 HUD 遮挡关系 |
| 1.21.2 Entry/Widget 渲染链拆分 | 成员列表行错位、内嵌按钮不响应 | 6.2 主界面 + 6.3 权限矩阵 |
| 1.21.5 mojmap 包重组 | 编译期暴露为主；残余风险为反射字段名遗漏 | L1 + L2（fake 熔断即暴露） |
| 1.21.6 渲染管线重构 | HUD 透明度异常、混合失效发黑/发白 | 6.2 G5 专项 |
| 1.21.6 KeyMapping 重构 | 键位不生效、分类显示为原始 key、冲突 | 6.2 平台层键位 |
| EditBox 构造变化 | 输入框 hint 丢失、maxlen 失效、光标异常 | 6.2 真实输入流 |
| FakeFont 6px 恒宽（失真源） | 离线断言通过但实机中文布局溢出 | 6.2 中文显示（每次必查） |
| execute 同步（失真源） | 离线通过但实机异步时序 bug（保存竞态） | 6.3 持久化 + 6.3 双端同步 |

### 6.5 测试问题处置约定

- 实机发现基础功能 bug：优先判断是「该组覆盖目录适配错误」还是「基线既有 bug」；前者修覆盖目录，后者回基线修复并同步评估各组回移。
- fake 熔断：逐组修复 fake（覆盖目录），修不了的字段（如被删除的 `Minecraft` 字段）允许在该组 fake 中降级 stub，但须保持既有断言语义不放松。
- 任何为通过测试而修改断言的行为，必须在提交 body 中说明依据（AGENTS.md 测试规范）。

## 7. 各兼容组适配要点

### G2（1.21.2 / 1.21.3）

- common：`drawString`/`drawCenteredString` float 化约 31 处；`Screen.render`/`Entry.render` 签名核对（`ProjectSettingsScreen` Entry 参数顺序）；`EditBox` 构造核对。
- fabric：`HudRenderCallback` → `HudLayerRegistrationCallback`（以 Fabric API 0.106+ 实际签名准，注意新 API 的层优先级参数对遮挡关系的影响）。
- neoforge：21.2 入口为构造器注入（mod bus/ModContainer）与 registerExtensionPoint 迁移；双总线划分与基线一致（实现 `net.neoforged.fml.event.IModBusEvent` 的注册类事件走 mod bus，游戏事件走 game bus），`NeoForgeNetworkBridge` 注册流程核对。
- 实机重点：6.4 表前三行。

### G3（1.21.4）

- 预期以矩阵参数与依赖版本升级为主；GUI 层核对 `GuiGraphics`/`EditBox` 无进一步签名破坏。
- ModMenu 升 13.x。实机按精简集（★）+ L5 单项。
- 实证结论（2026-09-06）：断点远小于初判，仅 `AbstractSelectionList.getScrollbarPosition()` 被移除 1 处（common 1 文件覆盖）；`EditBox` 构造恢复 6 参形态，v1_21_2 的 4 参覆盖不适用，基线直接通过；fabric-api 0.119.4 `HudRenderCallback` 仍保留（deprecated），fabric/neoforge 零覆盖。详见附录 B P2 状态。

### G4（1.21.5）

- mojmap 包重组：预计 common/fabric/neoforge 大量 import 与类名机械修复；以编译错误清单驱动。
- test 源集重点：`GuiTestSupport`/`FakeMinecraftClient` 反射字段与构造迁移（`DetectedVersion`、`GameNarrator`、`InputType` 等路径变化）。
- NeoForge 21.5 官方 Porting Primer 较长，`RenderGuiEvent`/网络注册逐条核对。
- 实证结论（2026-09-06，推翻初判：无 mojmap 包重组、无 fake 迁移）：首编 79 错误 = NBT API 大重构 62 处（`CompoundTag` get 系列返回 `Optional`，改用 `getStringOr`/`getIntOr`/`getLongOr`/`getBooleanOr` 缺省重载；`contains(key,type)`→`contains(key)`、`getList(key,type)`→`getListOrEmpty(key)`、`ListTag.getCompound(i)`→`getCompoundOrEmpty(i)`）+ 文本组件 14 处（`ClickEvent`/`HoverEvent` 变 interface + 嵌套 record：`new ClickEvent.RunCommand/…`、`new HoverEvent.ShowText(…)`；`MutableComponent.withClickEvent/withHoverEvent` 被移除但基线调用全在 `Style` 链内、`Style` 同名方法保留，无需结构改造）+ `RenderSystem.enableBlend/defaultBlendFunc` 移除 2 行（G5 预期断点提前出现）+ `getScrollbarPosition` 1 处（沿用 v1_21_4 覆盖）。test 源集断点仅 `PersistenceSafetyTestMain` 3 处 NBT 调用，`GuiTestSupport`/`FakeMinecraftClient` 等全部编译通过；fabric/neoforge 零覆盖。详见附录 B P3 状态。

### G5（1.21.6 / 1.21.7 / 1.21.8）

- `TodoHudRenderer` 移除/替换 `RenderSystem.enableBlend` 等三行调用（渲染管线由 `RenderPipeline` 管理，`GuiGraphics.fill` 自带 blend）。
- `KeyMapping` category 重构：fabric/neoforge 键位注册改造（每平台 3 键）。
- NeoForge 21.6 GUI 层重构核对 `RenderGuiEvent.Post`；ModMenu 升 15.x/16.x。
- 1.21.7/1.21.8 仅验证编译与 ★ 精简集 + L5 单项，预期无代码改动。
- 实证结论（2026-09-06，推翻初判：KeyMapping category 重构未出现、`RenderGuiEvent.Post` 无断点、ModMenu 三版本统一 15.0.2）：1.21.6 首编 88 错误 = v1_21_5 覆盖重现 ~77 处 + 新断点 4 类——① `RenderSystem.setShaderColor` 移除（随 1.21.5 渲染管线重构一并省略调用）；② `GuiGraphics.pose()` 返回 joml `Matrix3x2fStack`，`pushPose/popPose` → `pushMatrix/popMatrix`、`translate/scale` 收敛二维参数（`TodoHudRenderer`/`TodoScreenRenderSupport`）；③ `MultiLineEditBox` 构造器收窄为包私有，改用 `builder().…build(font, w, h, narration)`；④ test fake 断点（`LocalPlayer` 构造第 6 参 boolean→`Input`、`GameNarrator` say 系列更名、`SoundManager` 构造加 `MusicManager` 参且 `play` 返回 `SoundEngine.PlayResult`）。1.21.7 唯一新断点：NeoForge `PacketDistributor.sendToServer` 彻底移除，改经 vanilla `ServerboundCustomPayloadPacket` + `ClientPacketListener.send` 发送（21.6/21.7 通用）；1.21.8 零新增断点，v1_21_6 覆盖直接复用。详见附录 B P4 状态。
- 实机缺陷补记（2026-09-07，用户实机反馈，详见附录 B P4 状态）：① 编译期零断点但运行期崩溃——1.21.6 起 `Screen.renderWithTooltip` 框架层默认实现先调 `renderBackground`（含 blur），基线 `ConfigScreen.render` 内再手动调一次即触发 `Can only blur once per frame`，fabric/neoforge 双端同崩；② NeoForge 21.6 GUI 层重构后基线的 `RenderGuiEvent.Post` 挂接不渲染 HUD（fabric 端正常）；③ 配置界面配置项 label 文字空白、仅显示值——1.21.6 起 `GuiGraphics.drawString` 对 alpha==0 的颜色直接早退不渲染（javap 实证），基线 `0xFFFFFF` 等 RGB 字面量在旧版本依赖「默认补齐 alpha=255」兜底而 1.21.6+ 无此兜底；④ NeoForge 21.7/21.8 启动即崩（fabric 端正常）——21.7 起 `playBidirectional` 三参重载的 handler 仅注册为服务端侧，且客户端启动期 `ClientNetworkRegistry.setup` 校验 clientbound 载荷必须持有客户端 handler，缺失即抛 `IllegalStateException`；21.6 三参 handler 双侧生效且无启动校验故正常。①②③ 均通过 v1_21_6 覆盖修复（三版本共享覆盖组，一处修复生效），并新增 GUI 场景自测封堵「离线全绿、实机基础交互崩溃/渲染空白」盲区；④ 经同一覆盖文件改运行时反射探测四参重载修复（21.7+ 显式传双 handler、21.6 回退三参），启动校验通过需实机复验。

## 8. 分阶段落地计划

| 阶段 | 内容 | 退出条件 |
|------|------|----------|
| P0 基建 ✅ 已完成（2026-09-06 验证通过，见附录 B 状态） | 版本矩阵 + `build-local-121x.bat` + 覆盖源集机制 + 资源模板化；1.21.1 经矩阵路径全量回归 | 1.21.1 经新脚本产出的 jar 与现行 `build-local-21.bat` 等价；L1~L3 通过 |
| P1 ✅ 已完成（2026-09-06 离线验证通过，见附录 B 状态；L4~L6 实机回归待执行） | G2：1.21.2（全量 L1~L6）、1.21.3（L1~L3 + ★ + L5 单项） | 1.21.2/1.21.3 离线全绿（三端编译 + :common:check + build 产物入 dist\）；L4~L6 待实机执行 |
| P2 ✅ 已完成（2026-09-06 离线验证通过；同日用户实机验证 fabric/neoforge 双端通过） | G3：1.21.4（L1~L6，预期改动小） | 1.21.4 离线全绿（三端编译 + :common:check + build 产物入 dist\）；L4 实机通过（用户确认） |
| P3 ✅ 已完成（2026-09-06 离线验证通过；同日用户实机验证 fabric/neoforge 双端通过） | G4：1.21.5（L1~L6） | 1.21.5 离线全绿（三端编译 + :common:check + build 产物入 dist\）；L4 实机通过（用户确认） |
| P4 ✅ 已完成（2026-09-06 离线验证通过；2026-09-07 修复实机反馈的配置屏双 blur 崩溃、NeoForge HUD 不显示、配置项 label 空白、NeoForge 21.7/21.8 启动崩溃四类缺陷，并补 GUI 场景自测，产物已重建入 dist\，见附录 B；L4~L6 实机回归待执行） | G5：1.21.6（全量）、1.21.7/1.21.8（精简） | 1.21.6/1.21.7/1.21.8 离线全绿（三端编译 + :common:check + build 产物入 dist\）；实机缺陷修复后四版本 check 复验全绿；L4~L6 待实机复验 |
| P5（可选） | 1.21.9~1.21.11 评估立项；CI（release-precheck）矩阵扩展 | 另行方案 |

每组独立提交，提交规范遵循 AGENTS.md（跨模块/含背景时补 body）。

## 9. 风险与回退

- 占位版本号风险：附录 A 中「待校准」项必须在对应阶段开工前按附录 A 渠道核对，禁止凭记忆写死。
- 单分支回退：每个阶段为独立提交序列，可按阶段 revert；组覆盖目录相互隔离，不会交叉污染。
- 破坏性边界：`src/main/java`（G1 基线）在任何阶段都不允许改写为高版本 API——这是单分支多版本编译成立的前提（第 5 章边界 1）。
- 静默失真风险：离线全绿 ≠ 实机正常（第 2.2/6.4 节）；任何跳过实机回归的版本不得标记为可发布状态。
- 编码红线：全程 UTF-8；禁止 PowerShell 5.1 文本 cmdlet 读写源码（AGENTS.md 中文编码安全规则）。
- 大改动前备份：`TodoScreen.java`（约 4000 行）等大文件若涉及大量修改，先按 `.bak.YYYYMMDD-N` 规范备份。
- 已知存量乱码：`TodoListForge.java`、`NeoForgeNetworkBridge.java` 等文件中文注释存在历史乱码，移植/修改时注意不要基于乱码文本局部修补（AGENTS.md 规则 8）。

## 10. 协作约定更新（随 P0 落地）

- AGENTS.md 第 6 条追加：`1.21.x` 分支使用 `.\build-local-121x.bat <目标Minecraft版本> --offline`，全系列 Java 21。
- 本方案文档：`1.21.x-porting-plan.md`（仓库根目录），各阶段完成后更新状态标记。

## 附录 A：版本矩阵初稿

> `✓` = 已核实；`?` = 待校准（渠道见附录 A 末尾）。NeoForge 统一取各系列当时最新稳定 patch。

| MC 版本 | Java | Gradle | Loom | api_group | Fabric Loader | Fabric API | NeoForge | Forge | ModMenu | 状态 |
|---------|------|--------|------|-----------|---------------|------------|----------|-------|---------|------|
| 1.21.1 | 21 | 8.11.1 | 1.13.467 | v1_21_1 | 0.18.1 ✓ | 0.116.9+1.21.1 ✓ | 21.1.219 ✓ | 52.1.10 ✓ | 11.0.3 ✓ | 已发布基线 |
| 1.21.2 | 21 | 8.11.1 | 1.13.467 | v1_21_2 | 0.16.14 ✓ | 0.106.0+1.21.2 ✓ | 21.2.1-beta ✓ | 冻结 | 12.0.1 ? | 已适配（离线验证） |
| 1.21.3 | 21 | 8.11.1 | 1.13.467 | v1_21_2 | 0.16.14 ✓ | 0.107.0+1.21.3 ✓ | 21.3.97 ✓ | 冻结 | 12.0.1 ? | 已适配（离线验证） |
| 1.21.4 | 21 | 8.11.1 | 1.13.467 | v1_21_4 | 0.16.14 ✓ | 0.119.4+1.21.4 ✓ | 21.4.157 ✓ | 冻结 | 13.0.4 ✓ | 已适配（离线验证） |
| 1.21.5 | 21 | 8.11.1 | 1.13.467 | v1_21_5 | 0.16.14 ✓ | 0.128.2+1.21.5 ✓ | 21.5.97 ✓ | 冻结 | 14.0.2 ✓ | 已适配（离线验证） |
| 1.21.6 | 21 | 8.11.1 | 1.13.467 | v1_21_6 | 0.16.14 ✓ | 0.128.2+1.21.6 ✓ | 21.6.20-beta ✓ | 冻结 | 15.0.2 ✓ | 已适配（离线验证） |
| 1.21.7 | 21 | 8.11.1 | 1.13.467 | v1_21_6 | 0.16.14 ✓ | 0.129.0+1.21.7 ✓ | 21.7.25-beta ✓ | 冻结 | 15.0.2 ✓ | 已适配（离线验证） |
| 1.21.8 | 21 | 8.11.1 | 1.13.467 | v1_21_6 | 0.16.14 ✓ | 0.136.1+1.21.8 ✓ | 21.8.54 ✓ | 冻结 | 15.0.2 ✓ | 已适配（离线验证） |

版本号校准渠道（对应阶段开工前必查）：

- Fabric API / Loader：`https://maven.fabricmc.net/net/fabricmc/fabric-api/fabric-api/maven-metadata.xml`、`https://maven.fabricmc.net/net/fabricmc/fabric-loader/maven-metadata.xml`
- NeoForge：`https://maven.neoforged.net/releases/net/neoforged/neoforge/maven-metadata.xml`
- ModMenu：`https://maven.terraformersmc.com/releases/com/terraformersmc/modmenu/maven-metadata.xml`
- 各项目 Modrinth 版本页（核对 MC 对应关系）

## 附录 B：脚本与矩阵改造清单（P0 施工项）

1. `gradle/version-matrix.properties`：按附录 A 建 1.21.x 矩阵，字段沿用 1.20.x（`build_supported`、`minecraft_version`、`loader_version`、`fabric_api_version`、`neoforge_version`、`modmenu_version`、`fabric_loader_dependency`、`fabric_minecraft_dependency`、`minecraft_version_range`、`neoforge_loader_range`、`forge_supported`、`status`），新增 `api_group`，去掉 JDK/loom 切换字段（全系统一）。
2. `build-local-121x.bat`：参照 `build-local-120x.bat` 裁剪（固定 `JAVA_HOME=D:\JAVA\JDK\jdk-21.0.2`，透传 `-Ptarget_*` 与 `-Ptarget_api_group`，构建后拷贝 `dist\`）。
3. `common`/`fabric`/`neoforge` 的 `build.gradle.kts`：接入覆盖源集机制（第 4.2 节参考实现，含 test 源集）；`minecraft_version` 等属性读取改为 `findProperty("target_*") ?: property(...)` 兜底，保证不带参数时行为与现状一致。
4. 三个 loader 资源清单模板化 + `processResources` 注入（G2+ 的 forge 清单不注入、不发布）。
5. 根 `build.gradle.kts`：`forge_supported=false` 时 `distReleaseJars`/`h2JarContentCheck` 跳过 forge 产物（从 1.20.x 分支移植现成逻辑）。
6. AGENTS.md 第 6 条追加 1.21.x 构建入口。

> **P0 状态：已完成（2026-09-06）**。施工与验证结论：
>
> - 上述 6 项全部落地，另含 `settings.gradle.kts` 的 forge 条件 include（`target_forge_supported=false` 时排除 forge 子项目，未传属性默认包含，与 1.20.x 分支模式一致）。
> - 正向验证：`.\build-local-121x.bat 1.21.1 build --offline` → BUILD SUCCESSFUL（`:common:check` 12 项离线自测全过；三平台 jar 产出并拷入 `dist\`；`h2JarContentCheck` 三平台 `org/h2/Driver.class` 均为 1）。
> - 模板渲染正确：`fabric.mod.json`（`>=0.18.1` / `1.21.1`）、`mods.toml`（`[52,)` / `[1.21.1]`）、`neoforge.mods.toml`（`[21.1,)` / `[1.21.1]`）。
> - 负向验证：`1.21.2`（`build_supported=false`）被矩阵拒绝；`-Ptarget_forge_supported=false` 时 projects 列表不含 forge；无参数默认时四模块全包含（旧行为不变）。
> - 已知限制：AI 沙箱内禁止写 `E:\MC\modtest`（工作区外），脚本内 modtest 拷贝在沙箱中报「拒绝访问」，手动执行脚本不受影响；`dist\` 拷贝正常。
> - jar 等价性（与 `build-local-21.bat` 产物 diff）与 L4 实机回归留待 P1 开工前随 1.21.2 校准一并执行。

> **P1 状态：已完成（2026-09-06，L1~L3 离线验证通过）**。施工与验证结论：
>
> - G2 覆盖源集共 10 文件：common 6（`EditBox` 4 参构造 + `setPosition` 适配两处输入框；`MultiLineEditBox` 实证 1.21.2 仍为 7 参构造，descField 保持基线形态；42 处 `drawString` 依赖 int→float 隐式转换零覆盖）、fabric 1（`TodoClient` HUD 回调第二参数 float→`DeltaTracker`）、neoforge 3（构造器注入 mod bus/ModContainer；`registerExtensionPoint` 移至 `ModContainer` 并显式 cast `IConfigScreenFactory` 消除重载歧义）。
> - 版本校准实证修正（推翻方案初判）：1.21.2 fabric-api 0.106.0+1.21.2 仍提供 `HudRenderCallback`（无分层 HUD 子包，方法签名经 jar 字节码实证）；NeoForge 21.3 无 21.3.0 稳定版，取 21.3.97；21.2 系列无稳定版，取 21.2.1-beta。
> - 覆盖源集机制两处兼容修复：① srcDirs 只放目录 + `filter.exclude` 按绝对路径排除基线被覆盖文件（loom 校验 srcDirs 必须为目录，fileTree 方案与其冲突）；② `allSource.filter` 同步排除——`withSourcesJar` 的 sourcesJar 消费 `allSource`，`source()` 仅复制 srcDirs 不复制 filter，不同步排除会因基线/覆盖同名文件重复打包而失败。另根 `distReleaseJars`/`h2JarContentCheck` 按 `forgeEnabled` 条件化（矩阵排除 forge 时不再硬引用 `:forge`）。
> - 正向验证：`.\build-local-121x.bat 1.21.2 build --offline` 与 `1.21.3 build --offline` 均 BUILD SUCCESSFUL；`:common:check` 12 项离线自测全过；fabric/neoforge jar 产出并拷入 `dist\`，`h2JarContentCheck` 双平台 `org/h2/Driver.class` 均为 1。
> - G2 兼容组假设成立：1.21.3 零专属覆盖代码，与 1.21.2 共用 v1_21_2 覆盖源集，三端编译一次通过。
> - L4 实机回归修正（2026-09-06）：v1_21_2 neoforge 覆盖曾错误把 game 事件（玩家登录/命令注册/服务器生命周期/客户端 tick/GUI 渲染/网络登录登出）一并迁至 modEventBus，导致 1.21.2/1.21.3 NeoForge 端启动即崩（IllegalArgumentException: not valid for this bus），fabric 端正常。已全部恢复 `NeoForge.EVENT_BUS` 注册；21.2 双总线划分与基线一致（实现 `net.neoforged.fml.event.IModBusEvent` 走 mod bus，其余走 game bus），涉及的 10 个事件已逐一经 21.2.1-beta 字节码实证。教训：`addListener` 编译期不校验事件与 bus 匹配，离线全绿无法暴露此类错误。
> - 待办交接（用户实机执行）：L4 复验（bus 修复后的 NeoForge jar 已重建入 `dist\`，fabric jar 无变化）、1.21.2 全量 L5/L6、1.21.3 L5 单项；1.21.2 NeoForge 为 21.2.1-beta，实机验证时留意 beta 行为。ModMenu 12.0.1 因离线构建禁用集成未实证，首次在线构建或实机验证时确认。
> - P0 遗留项说明：jar 等价性 diff（与 `build-local-21.bat` 产物）仍未执行——G2 覆盖改动不影响 v1_21_1 路径产物（api_group=v1_21_1 时不进覆盖分支），可在 1.21.1 任意复验。

> **P2 状态：已完成（2026-09-06，L1~L3 离线验证通过）**。施工与验证结论：
>
> - 版本校准：NeoForge 取 21.4.157（官方 maven pom 核验存在；maven-metadata.xml 过大被截断、promotions_slim.json 已 404，经第三方镜像定位后回官方源核验）；fabric-api 取 0.119.4+1.21.4（Modrinth 最新 release）；loader 沿用 0.16.14。
> - 断点实证（推翻 G3 初判，实际仅 1 处代码断点）：`EditBox` 构造在 1.21.4 恢复 6 参 `(Font, x, y, w, h, Component)`（Mojang 回退了 1.21.2 的 4 参+`setPosition` 实验，另有 4 参/7 参重载），v1_21_2 的 4 参覆盖不适用，基线形态直接编译通过；`MultiLineEditBox` 仍为 7 参；`AbstractSelectionList.getScrollbarPosition()` 被移除（ObjectSelectionList 亦无，滚动条重构为内部 `renderScrollbar`/`scrollBarX`）；fabric-api 0.119.4 `HudRenderCallback` 仍保留（0.116.0 起标记 deprecated），HUD 基线形态编译通过；NeoForge 21.4.157 入口仍为基线 1.21.1 形态。
> - G3 覆盖源集共 1 文件：common 1（`ProjectSettingsScreen` 基线全文复制，删除 `getScrollbarPosition` 覆盖）；fabric 0、neoforge 0。
> - 正向验证：`.\build-local-121x.bat 1.21.4 build --offline` BUILD SUCCESSFUL；`:common:check` 12 项离线自测全过；fabric/neoforge jar 产出并拷入 `dist\`。
> - 待办交接（用户实机执行）：L4 实机回归（fabric + neoforge jar 均为新增产物）、★ 精简集 + L5 单项；ModMenu 13.0.4 因离线构建禁用集成未实证，首次在线构建或实机验证时确认。
> - 实机结论（2026-09-06）：用户确认 1.21.4 fabric/neoforge 双端实机验证通过，目前无问题。

> **P3 状态：已完成（2026-09-06，L1~L3 离线验证通过）**。施工与验证结论：
>
> - 版本校准：NeoForge 取 21.5.97（第三方镜像 maven-metadata 定位 + 官方 maven pom 存在性核验）；fabric-api 取 0.128.2+1.21.5（Modrinth 最新 release，推翻初稿 0.121.0 首发版）；loader 沿用 0.16.14。
> - 断点实证（首编 79 错误全量归因，编译错误驱动逐条消解）：① NBT 大重构 62 处（`getString/getInt/getLong/getBoolean` 返回 `Optional` → `getStringOr(key, "")` 等缺省重载，与旧 API 缺省值精确等价；`contains(key,type)` → `contains(key)`；`getList(key,type)` → `getListOrEmpty(key)`；`ListTag.getCompound(i)` → `getCompoundOrEmpty(i)`）；② 文本组件 14 处（`ClickEvent`/`HoverEvent` 从类改为 interface + 嵌套 record 实现，`new ClickEvent(Action.RUN_COMMAND, cmd)` → `new ClickEvent.RunCommand(cmd)` 等四种子类型；`withClickEvent/withHoverEvent` 缺失错误被参数表达式错误掩盖，经 javac 探针实证确实移除，但基线调用全在 `.withStyle(style -> …)` 的 `Style` 链内、`Style.withClickEvent/withHoverEvent` 保留，仅替换构造表达式即可）；③ `RenderSystem.enableBlend()/defaultBlendFunc()` 移除 2 行（1.21.5 渲染管线统一管理混合状态）；④ `getScrollbarPosition` 沿用 v1_21_4 覆盖。
> - v1_21_5 覆盖源集共 11 文件：common main 10（`Task`/`TaskStorage`/`Project`/`ProjectStorage`/`ProjectPlayerStateStorage`/`H2LegacyMigrationReader`/`CommandBootstrap`/`ProjectPackets`/`TodoHudRenderer`/`ProjectSettingsScreen`）+ common test 1（`PersistenceSafetyTestMain` 仅 3 处 NBT 调用；`GuiTestSupport`/`FakeMinecraftClient` 等 fake 类在 1.21.5 全部编译通过，初判的 `DetectedVersion`/`GameNarrator`/`InputType` fake 迁移断点不存在）；fabric 0、neoforge 0。
> - 正向验证：`:common:compileJava` + `:fabric:compileJava` + `:neoforge:compileJava`、`:common:compileTestJava`、`:common:check`（12 项自测 544 用例 PASS、0 FAIL）、`.\build-local-121x.bat 1.21.5 build --offline` 均 BUILD SUCCESSFUL；fabric/neoforge jar 产出并拷入 `dist\`（todolist-fabric/neoforge-1.21.5-1.4.1.jar）。
> - 待办交接（用户实机执行）：L4 实机回归 + ★ 精简集 + L5 单项 + L6 双路径（G4 为组首版本必做）；ModMenu 14.0.2 因离线构建禁用集成未实证，首次在线构建或实机验证时确认。
> - 实机结论（2026-09-06）：用户确认 1.21.5 fabric/neoforge 双端实机验证通过，目前无问题。

> **P4 状态：已完成（2026-09-06，L1~L3 离线验证通过）**。施工与验证结论：
>
> - 版本校准：NeoForge 21.6/21.7 系列经官方 maven-metadata 实证均无稳定版（beta-only），取 21.6.20-beta / 21.7.25-beta，21.8 系列取稳定版 21.8.54；fabric-api 取 0.128.2+1.21.6 / 0.129.0+1.21.7 / 0.136.1+1.21.8；loader 沿用 0.16.14；ModMenu 三版本统一 15.0.2（16.x 面向 1.21.9+，推翻初稿 1.21.8 用 16.0.1 的假设）。
>
> - 断点实证（1.21.6 首编 88 错误全量归因，~77 处为 v1_21_5 覆盖重现直接复制消解）：新断点 4 类——① `RenderSystem.setShaderColor(float×4)` 移除（HUD 标题描边着色，省略调用）；② `GuiGraphics.pose()` 改返 joml `Matrix3x2fStack`：`pushPose/popPose` → `pushMatrix/popMatrix`、`translate/scale` 三参收敛二参（`TodoHudRenderer` 缩放绘制 + `TodoScreenRenderSupport` 标题缩放两处）；③ `MultiLineEditBox` 构造器收窄为包私有，改 `MultiLineEditBox.builder().…build(font, w, h, narration)` 构建（`TodoScreenWidgetBuildSupport`）；④ test fake 三件（`FakeClientPlayer`：`LocalPlayer` 构造第 6 参 boolean→`Input`；`FakeGameNarrator`：say/sayChat/sayNow 更名为 sayChatQueued/saySystemChatQueued/saySystemQueued/saySystemNow；`FakeSoundManager`：构造新增 `MusicManager` 参、`play` 返回 `SoundEngine.PlayResult`）。compileJava 一次通过，compileTestJava 补 3 个 fake 覆盖后二次通过。
>
> - 1.21.7 唯一新断点：NeoForge 21.7 彻底移除 `PacketDistributor.sendToServer`（internal `NeoForgeProxy.sendToServer` 一并移除，经 neoforge-merged jar javap + sources jar 双实证），`NeoForgeNetworkBridge` 覆盖改经 vanilla `ServerboundCustomPayloadPacket` + `ClientCommonPacketListenerImpl.send(Packet<?>)` 直接发送（vanilla 公开 API，21.6/21.7 通用，与 NeoForge 自身 `GenericPacketSplitter` 构造方式一致）；`sendToPlayer` 等服务端方法 21.7 保留，覆盖仅改 `sendToServer`。1.21.8 零新增断点，v1_21_6 覆盖（含 neoforge 覆盖）三版本共用，一次编译通过。
>
> - v1_21_6 覆盖源集共 17 文件：common main 12（`ProjectPackets`/`CommandBootstrap`/`H2LegacyMigrationReader`/`ProjectPlayerStateStorage`/`ProjectStorage`/`Project`/`TaskStorage`/`Task` 8 文件复制自 v1_21_5；`TodoHudRenderer`/`TodoScreenRenderSupport` pose 栈补丁；`TodoScreenWidgetBuildSupport` Builder 化；`ProjectSettingsScreen` 沿用）+ common test 4（`PersistenceSafetyTestMain` 沿用 + `FakeClientPlayer`/`FakeGameNarrator`/`FakeSoundManager` 新增覆盖）+ neoforge main 1（`NeoForgeNetworkBridge`）。
>
> - 正向验证：1.21.6/1.21.7/1.21.8 各自 `:common:compileJava` + `:fabric:compileJava` + `:neoforge:compileJava` + `:common:compileTestJava` + `:common:check`（12 项自测 566 用例 PASS、0 FAIL）+ `.\build-local-121x.bat <版本> build --offline` 均 BUILD SUCCESSFUL；六件 jar 产出并拷入 `dist\`（fabric/neoforge × 1.21.6/1.21.7/1.21.8）。
>
> - 待办交接（用户实机执行）：1.21.6 L4 全量 + ★ 精简集 + L5 单项 + L6 双路径（组首版本必做）；1.21.7/1.21.8 L4（★ 精简集）+ L5 单项；1.21.6/1.21.7 NeoForge 为 beta 版（21.6.20-beta / 21.7.25-beta），实机验证时留意 beta 行为；ModMenu 15.0.2 因离线构建禁用集成未实证，首次在线构建或实机验证时确认。
>
> **P4 补充：实机缺陷修复（2026-09-07）**。用户实机反馈四类缺陷 + 测试补强，全部经 v1_21_6 覆盖修复（1.21.6/1.21.7/1.21.8 共享覆盖组，javap 实证三版本 `renderWithTooltip` 字节码逐字节一致）：
>
> - 缺陷①（配置屏崩溃，fabric/neoforge 双端）：1.21.6 起 `Screen.renderWithTooltip` 框架层默认实现为 `nextStratum → renderBackground（blur #1）→ nextStratum → render`，基线 `ConfigScreen.render` 内手动再调 `renderBackground` 构成同帧第二次 blur，`GuiRenderState.blurBeforeThisStratum` 抛 `IllegalStateException: Can only blur once per frame`。修复：`common/src/main/v1_21_6/java/com/todolist/gui/ConfigScreen.java` 覆盖（基线整文件复制，render 内删除手动 `renderBackground` 调用）；grep 实证基线仅 ConfigScreen 一处此类调用，其余 6 屏 render 均 override 自绘无同类问题。
>
> - 缺陷②（NeoForge HUD 不显示，fabric 端正常）：NeoForge 21.6 GUI 层重构后基线经 `RenderGuiEvent.Post`（game 总线）挂接的 HUD 实机不触发。修复：`neoforge/src/main/v1_21_6/java/com/todolist/client/NeoForgeTodoClient.java` 覆盖，改用 21.6 官方层体系 `RegisterGuiLayersEvent.registerAboveAll`（mod 总线）注册 `todolist:todo_hud` 层，同时移除 `RenderGuiEvent.Post` 监听避免层+事件双渲染，并加首次渲染 info 日志便于实机区分「层未触发」与「触发后早退」。
>
> - 缺陷③（配置项 label 文字空白、仅显示值，fabric/neoforge 双端，用户实机 2026-09-07 反馈）：1.21.6 起 `GuiGraphics.drawString` 各重载开头均为 `ARGB.alpha(color)==0 → return` 早退（javap 实证，diag-p4r14 日志），`0xFFFFFF`（alpha=0x00）的文字直接跳过渲染；1.21.1~1.21.5 旧管线对 RGB 颜色默认补齐 alpha=255，故无此兜底后基线字面量全部失效。受影响面（全库 grep 实证）：ConfigScreen 3 处 `0xFFFFFF`（预览标题/提示 + `drawLabelForWidget`，后者即 label 空白主症状）；HUD 因 `toOpaqueColor` 显式补 alpha 不受影响（仅统一其参数写法）。修复：v1_21_6 覆盖 ConfigScreen 3 处补 `0xFFFFFFFF`；覆盖 TodoHudRenderer 7 处 `toOpaqueColor` 参数统一带 `0xFF` 前缀（掩码截断语义下行为等价，纯写法统一便于守卫扫描）。
>
> - 缺陷④（NeoForge 21.7/21.8 启动即崩，fabric 端正常，用户实机 2026-09-07 反馈）：崩溃落在 `net.neoforged.neoforge.client.network.registration.ClientNetworkRegistry.setup`，抛 `IllegalStateException: Some clientbound payloads are missing client-side handlers: [todolist:bridge]`。sources jar 实证成因：NeoForge 21.7 起 `PayloadRegistrar.playBidirectional(type, codec, handler)` 三参重载语义漂移——handler 仅注册为服务端侧（内部委托四参形态且 clientHandler 传 null），同时 21.7/21.8 新增客户端启动期校验：`ClientNetworkRegistry.setup` 要求所有 clientbound 已注册载荷必须持有客户端 handler，缺失即抛异常阻止游戏启动；21.6 仅有三参形态（handler 双侧生效）且无 `ClientNetworkRegistry` 校验类，故 1.21.6 实机正常。`playToServer` + `playToClient` 同 TYPE 拆分注册方案不可行（21.6/21.7 的 `NetworkRegistry.register` 均对同 id 重复注册抛 `UnsupportedOperationException`）。修复：v1_21_6 覆盖 `NeoForgeNetworkBridge.onRegisterPayloadHandlers` 运行时反射探测四参 `playBidirectional(type, codec, serverHandler, clientHandler)`——21.7/21.8 调四参显式传双 handler，21.6 无四参方法回退三参保持旧行为；三参形态在 21.6/21.7/21.8 均保留，同一份覆盖源码三版本均可编译；同步重写该方法上方历史遗留乱码 javadoc。
>
> - 测试补强（回应「按钮点击、输入框输入、HUD 展示应有场景测试」）：新增 `common/src/test/java/com/todolist/gui/GuiScenarioTestMain.java` 五场景——① 坐标级 mouseClicked/mouseReleased 点击配置按钮断言状态翻转；② EditBox 真实键盘路径（点击聚焦 → charTyped 逐字符 → BACKSPACE → 追加 → ENTER）断言内容变化；③ 双 blur 结构守卫：运行时探测框架 `renderWithTooltip` 是否内调 `renderBackground`，据此断言 ConfigScreen.render 不得/必须自行调用（1.21.1~1.21.5 与 1.21.6+ 自动分支），其余 6 屏 render 一律不得调用；④ 渲染方法引用守卫：扫描 HUD 渲染器 + 7 屏字节码 invoke 引用，验证非 JDK 引用在运行时 classpath 可解析（捕获渲染 API 移除导致的 `NoSuchMethodError` 类别）；⑤ 文字颜色 alpha 完整性守卫：运行时探测框架 `drawString` 是否引用 `ARGB.alpha`（即 1.21.6+ 早退语义），仅在新语义下扫描 HUD 渲染器 + 7 屏 + 双列表控件字节码的 ldc 整数常量，断言不存在无 alpha 的灰白颜色形态（R==G==B、值域 0x101010~0xFFFFFF）；掩码用途（ldc 后紧跟 `iand`，如 `color & 0x00FFFFFF`）自动过滤不误报。配套新增/扩展 `ClassFileMethodScanner`（无依赖 class 字节码扫描器：invoke 引用 + ldc 整数常量 + 掩码过滤）。
>
> - 测试基建（真实键盘路径的 GLFW 依赖）：`EditBox.onClick/keyPressed → Screen.hasShiftDown → InputConstants/GLFW` 需加载 LWJGL natives。`guiSystemTest` classpath 追加 loom `minecraftNatives` 配置（LWJGL 3.3 支持从 classpath natives jar 提取共享库，随 MC 版本自动匹配）；`GuiTestSupport` 为 Fake Window 注入非零假句柄绕开 LWJGL `Checks.check` 空指针拒绝（GLFW 未初始化时 `glfwGetKey` 在 C 层安全返回 RELEASE，不创建窗口）。
>
> - 复验结论：缺陷①②修复后 1.21.1/1.21.6/1.21.7/1.21.8 `:common:check` 全绿（1.21.6 起 ③ 走「框架已渲染背景」分支 PASS，即崩溃修复的行为级验证）；1.21.6 `:neoforge:build` 编译通过；三版本全量 build 重建六件 jar 入 `dist\`。缺陷③修复后四版本 `:common:check` 复验再次全绿（1.21.6/7/8 场景⑤ 在早退语义下扫描 10 个渲染类全部干净，1.21.1 走「无早退语义」跳过分支），三版本全量 build 第二次重建六件 jar 入 `dist\`。已知非阻塞：1.21.7 `commandSystemTest` 的 `TaskPacketsPermissionTestMain.shouldAllowNonMemberClaimUpdateWhenAllPlayerClaimCompleteEnabled` 出现过一次 flaky 失败（重跑两次稳定通过，与本次改动无关，待观察）。缺陷④修复后 1.21.6/1.21.7/1.21.8 `:neoforge:compileJava` 编译通过（21.6 三参回退路径 + 反射代码对 21.7/21.8 API），三版本全量 build 第三次重建六件 jar 入 `dist\`；反射探测下的启动校验行为离线无法模拟（需 NeoForge 客户端真实启动流程触发 `ClientNetworkRegistry.setup`），留待实机复验。
>
> - 待办交接（用户实机执行）：1.21.6~1.21.8 复验——配置屏打开/关闭不再崩溃且**配置项 label 文字正常显示**，NeoForge HUD 显示并观察日志 `Todo List HUD layer first render triggered`；**1.21.7/1.21.8 NeoForge 端启动不再报 `missing client-side handlers`（缺陷④反射四参路径），1.21.6 NeoForge 回归启动正常（反射回退三参路径）**；构建脚本对 `E:\MC\modtest` 各 mods 目录的自动拷贝因沙箱权限未执行，实机验证前需从 `dist\` 手动拷贝（或由用户在沙箱外重跑 build 脚本）。
