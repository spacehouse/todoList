# 1.21.9 ~ 1.21.11 适配方案（分支 1.21.9-1.21.11）

> 状态：方案定稿，待评审。
> 基线：`1.21.x` 分支 HEAD（`2b2b1f9`，P4 全部完成、实机四缺陷闭环、工作区干净）。
> 本文阶段代号（P0~P5）与《1.21.x-porting-plan.md》相互独立，两文档各管各的分支。

## 1. 背景与分支策略

- 1.21.9 起的断点性质是**工具链级**（loom/Gradle 升级、fabric 源集结构、FML 重构），超出 1.21.x 分支覆盖组机制的管理范围；1.21.11 再叠加 vanilla API 大规模重命名（`ResourceLocation` → `Identifier`）。
- 若在 1.21.x 分支续做：需实现 per-profile loom 多版本机制（settings 级 hack，复杂脆弱），且 1.21.1~1.21.8 八个已验证 profile 全部回归重测。
- **决策**：从 1.21.x HEAD 切出 `1.21.9-1.21.11` 分支，只承载 1.21.9 / 1.21.10 / 1.21.11 三个版本；`1.21.x` 分支冻结为维护态（1.21.1~1.21.8，只收 bug fix）。
- **fix 同步策略**：common 中版本无关的业务修复（存储、H2、任务模型）直接 cherry-pick；网络/GUI 覆盖层文件因两分支结构不同，按需手工同步。
- 1.21.11 是最后一个 `1.x` 版本（2026 起 Mojang 改用 `26.1` 版本号制 + Java 25 + 去混淆），本分支即 1.x 系终点线，后续 26.x 另行立项。
- `forge/` 子项目（仅 1.21.1 语义）在本分支不参与构建，目录保留不动；如需清理另行确认。

## 2. 三版本生态概况

| 项 | 1.21.9 | 1.21.10 | 1.21.11 |
|---|---|---|---|
| 发布 | 2025-09-30（铜器时代） | 2025-10 中旬（1.21.9 修复版） | 2025-12-09（Mounts of Mayhem） |
| 协议 / 数据版本 | 773 / 4554 | calibrate（预期微增） | 774 / 4671 |
| 资源包 / 数据包格式 | 69.0 / 88.0 | calibrate | 75.0 / 94.1 |
| 最低 Java | 21（不变，26.1 才要 25） | 21 | 21 |
| NeoForge | 21.9.x（beta 起步，终版 calibrate） | 21.10.x | 21.11.x（beta 起步） |
| fabric-api / loader / ModMenu | calibrate；ModMenu 16.0.x | 沿用 1.21.9 组 | calibrate（ModMenu 或 17.x） |
| 断点量级 | 中偏重（工具链+FML+渲染） | 轻（搭 1.21.9 便车） | 重（vanilla 重命名潮） |

生态关键事实（官方渠道实证）：
- NeoForge 21.9：FML 启动/类加载重写（`FMLEnvironment.dist` → `getDist()` 等）、Transfer API 重构、世界渲染分相重构（本项目仅 HUD，不涉实体渲染器）。
- NeoForge 21.11：`ResourceLocation` → `Identifier`、JSpecify 空注解、`RenderType` → `RenderTypes`、`Util` 改名。
- 1.21.9 vanilla：着色器升级 OpenGL 3.3、post pass 合并重构（`post/blur.vsh` 等移除）——影响 vanilla blur 背景链。
- fabric 生态：loom 9.x 线要求新构建写法；client 类仅 client 源集可见（需评估拆 `src/client/java`）；yarn 停更转 Mojang 官方映射（**本项目 G5 起已用 `officialMojangMappings()`，天然免疫**）。

## 3. 断层分析（对照本项目代码实测）

| 变化 | 本项目影响面（实测） | 量级 |
|---|---|---|
| FML `FMLEnvironment.dist` → `getDist()` | neoforge 主源集 `TodoListNeoForge.java` 1 处 | 小 |
| `ResourceLocation` → `Identifier`（1.21.11） | 全仓 178 处 / 21 文件（剔除 forge 约 160 / 17），含 common 主源集 `TaskPackets`（26）、`ProjectPackets`（31） | **最大单项** |
| JSpecify 替换 javax 注解 | `javax.annotation.Nullable` 全仓 **0 处** | 无 |
| `RenderType`→`RenderTypes`、`Util` 改名 | GUI/HUD 未直接引用，编译期确认 | 预计无 |
| 着色器/blur 重构 | ConfigScreen 依赖 vanilla blur（P4 缺陷①③敏感点） | 行为风险，实机验证 |
| NeoForge 网络 API | 无 PayloadRegistrar 再变公告；反射探测四参方案预计延续 | P1/P4 sources 复核 |
| fabric client 源集隔离 | fabric 客户端类全在 main 源集 | **结构性风险** |
| Transfer API / gamerule 注册表化 / 世界渲染分相 | 不涉及（无物品流体、无 gamerule、无实体渲染器） | 无 |
| GuiGraphics API | 全仓 48 处 / 15 文件；无签名变化公告 | 编译期确认 |

## 4. 目标架构

### 4.1 源集：主源集前滚到 1.21.9 语义

核心红利：新分支最低支持 1.21.9，**所有「直接改主源集」的适配都合法化**，不再需要向下兼容 1.21.1。

机制核实结论（P0 实证）：覆盖源集为**单组、无级联**——`api_group=v1_21_6` 时仅 `src/<sourceSet>/v1_21_6/java` 参与覆盖，v1_21_2/4/5 组不叠加；且 fabric 自 1.21.4 起无组目录（纯基线）、neoforge 的 v1_21_2 组文件自 1.21.6 起不参与构建。

- 吸收原则 = **将 1.21.6~1.21.8 profile 的实际编译源（基线 + v1_21_6 组）固化为主源集**：v1_21_6 组 19 文件替换主源集同名文件（common main 13 + common test 4 + neoforge main 2），其余文件（含 fabric 全部、neoforge 的 `TodoListNeoForge` 基线版）保持不动；
- `TodoListNeoForge` 保持基线版（`FMLEnvironment.dist`，1.21.6~1.21.8 现状即如此），P2 随 1.21.9 直接改 `getDist()`；
- fabric 主源集保持基线（`HudRenderCallback` 版，1.21.6~1.21.8 现状即如此），P2 按 1.21.9 fabric-api 实际 API 修正；
- 全部旧覆盖组目录删除（共 9 个，见 §5，旧组仅服务新分支不支持的 1.21.2~1.21.5），覆盖机制代码（build.gradle.kts）保持不动备用。

### 4.2 api_group 规划

| profile | api_group | 说明 |
|---|---|---|
| 1.21.9 | `v1_21_1`（无覆盖） | 主源集即 1.21.9 语义 |
| 1.21.10 | `v1_21_1`（无覆盖） | 预期搭车；若有微断点再升组 |
| 1.21.11 | `v1_21_11`（新组） | 仅处理 Identifier 重命名等 21.11 特有差异 |

v1_21_11 组预计需覆盖：common（`TaskPackets`、`ProjectPackets` 等）+ fabric 6 文件 + neoforge 5 文件 ≈ 13~17 文件（P4 阶段按编译错误精确圈定）。

### 4.3 构建链

- architectury-loom：1.13.467 → **1.17.491**（P1 在线实录：`maven.architectury.dev` / `maven.shedaniel.me` 的 maven-metadata 实证版本线为 1.13.457~469 → 1.14.471~476 → 1.17.477~491，**1.15/1.16 线不存在**，原文假设作废；1.17.491 为线终版）；Gradle 8.11.1 → **9.5.0**（与 loom 1.17 自身 wrapper 一致）。
- 附带修正（P1）：删除根 `build.gradle.kts` 的 Modrinth maven 仓库声明——`maven.modrinth.com` DNS 已全球失效（Modrinth 现行规范 URL 为 `api.modrinth.com/maven`），且本项目无任何依赖使用该仓库（modmenu 走 Terraformers 官方 maven、fabric 系走 fabricmc、NeoForge 走官方 releases）；旧声明为模板遗留死配置，在线首跑即触发致命解析错误（Gradle 对主机不可达不跳过后续仓库）。
- Java 21 工具链不变。
- `version-matrix.properties` 重置为仅三 profile（1.21.1~1.21.8 条目删除），版本值 P1 在线校准前 `build_supported=false` 占位。
- `build-local-121x.bat` 沿用（其按 matrix 驱动；P1 核验脚本内无硬编码旧版本列表）。

### 4.4 fabric 源集结构（P1 决策点）

loom 9.x + Mojang client jar 拆分后，fabric 端客户端类可能仅 client 源集可见。P1 做最小验证实验后三选一：
1. 拆 `src/client/java`（迁移 `TodoClient`、`ClientTaskPackets`、`ClientProjectPackets` 等客户端类）；
2. 确认 architectury-loom 允许 main 源集继续引用 client 类（则不动）；
3. 折中：仅迁移编译报错的类。

## 5. 主源集吸收映射表（P0 执行清单）

**替换主源集同名文件（19 个 = v1_21_6 组全部文件，源=覆盖组终态，字节级复制）**：

| 源组 | 文件 |
|---|---|
| common main v1_21_6（13） | `gui/ConfigScreen`、`gui/TodoScreenWidgetBuildSupport`、`gui/TodoScreenRenderSupport`、`gui/ProjectSettingsScreen`、`client/TodoHudRenderer`、`task/TaskStorage`、`task/Task`、`storage/H2LegacyMigrationReader`、`project/ProjectStorage`、`project/ProjectPlayerStateStorage`、`project/Project`、`network/ProjectPackets`、`bootstrap/CommandBootstrap` |
| common test v1_21_6（4） | `gui/PersistenceSafetyTestMain`、`gui/testsupport/FakeClientPlayer`、`gui/testsupport/FakeGameNarrator`、`gui/testsupport/FakeSoundManager` |
| neoforge main v1_21_6（2） | `neoforge/network/NeoForgeNetworkBridge`（反射四参版）、`client/NeoForgeTodoClient`（RegisterGuiLayersEvent 版） |

不吸收（保持基线不动，与 1.21.6~1.21.8 实际编译源一致）：fabric 全部文件（含 `TodoClient` 的 `HudRenderCallback` 基线版）、neoforge `TodoListNeoForge` 基线版、common 主源集未被 v1_21_6 组覆盖的文件（含 `AssignPlayerScreen`/`AddProjectScreen`/`AddMemberScreen` 基线版——1.21.4~1.21.8 实际使用基线版且编译运行正常）。

**吸收验证（P0 必做）**：复制后逐文件比对源与目标字节一致（shutil 字节级复制 + 长度/内容校验）；吸收完成态等价于 1.21.8 profile 的编译源组合（基线 + v1_21_6 组展开）。

**删除目录（9 个，删除前征求确认）**：
- `common/src/main/v1_21_2`、`v1_21_4`、`v1_21_5`、`v1_21_6`
- `common/src/test/v1_21_5`、`v1_21_6`
- `fabric/src/main/v1_21_2`
- `neoforge/src/main/v1_21_2`、`v1_21_6`

旧组内容在 git 历史中永久可查（1.21.x 分支），删除仅影响新分支工作区。

**顺手清理**：吸收后对 common/neoforge 主源集全量扫描乱码特征（`鍒`/`闂`/`锟`/`�` 等），重写基线历史遗留乱码注释（v1_21_6 `NeoForgeNetworkBridge` 的方法体已修，其余同类问题一并处理）。

## 6. 阶段计划

| 阶段 | 内容 | 产出 / 验证 | 网络需求 |
|---|---|---|---|
| P0 分支基建 | 分支已建；§5 吸收映射执行 + 累积性 diff 验证 + 旧组删除 + 乱码清理 + 矩阵重置（三 profile 占位）+ 旧 plan 文档头部标注冻结 | git 提交（chore(port)）；无构建验证（版本未校准，编译必失败属预期） | 离线 |
| P1 工具链与校准 | loom/Gradle 升级；三版本依赖（fabric-api/loader/ModMenu/NeoForge）在线校准入矩阵；fabric 源集结构决策（§4.4）；1.21.9 profile `build_supported=true` | `:fabric:compileJava`/`:neoforge:compileJava` 首次通过 | **在线** |
| P2 1.21.9 适配 | 主源集按编译错误修正（FML getDist、GuiGraphics 漂移等）；NeoForge 21.9 sources 网络层复核（反射四参仍有效）；**新增网络反射探测离线测试**（§8.2-1）；fake 类随 vanilla 签名漂移更新 | `:common:check`（含场景测试）+ 三子项目 build + dist + 实机（P4 四缺陷全量回归） | 离线（构建） |
| P3 1.21.10 搭车 | 矩阵加 profile、版本校准、编译确认 | build + 实机抽查 | 在线（校准） |
| P4 1.21.11 适配 | 建 `v1_21_11` 覆盖组（Identifier 等，按编译错误圈定 13~17 文件）；NeoForge 21.11 sources 复核；**新增通道 id 守卫测试**（§8.2-2）；blur/着色器行为实机验证 | check + build + dist + 实机 | 在线（校准）+ 离线（构建） |
| P5 收尾 | 本文档更新为实录、dist 汇总、1.21.x 分支冻结说明 | 全部提交完成 | 离线 |

## 7. 风险与 calibrate 清单

**calibrate（P1/P3/P4 在线执行，附录 A 渠道：maven-metadata / CurseForge / Modrinth / 官方公告）**：
1. ✅（P1）architectury-loom 支持 1.21.9~1.21.11 的精确版本线及最低 Gradle 要求：**1.17.491 + Gradle 9.5.0**（版本线 1.13→1.14→1.17，1.15/1.16 不存在；来源 `maven.architectury.dev` / `maven.shedaniel.me` metadata，两者 148 版本完全同步；目录 autoindex 显示停滞系过时假象）；
2. ✅（P1）fabric-api：1.21.9=`0.134.1+1.21.9`、1.21.10=`0.138.4+1.21.10`、1.21.11=`0.141.6+1.21.11`（Modrinth API）；loader 定夺 **0.18.1**（1.21.x 分支实测线；0.19.5 为 26.x 时代准备），运行时 depends 保持宽松 `>=0.16.14`；
3. ✅（P1）NeoForge（官方 maven POM GET 200 权威确认）：1.21.9=`21.9.16-beta`（线终版仍 beta；FML 10.0.14）、1.21.10=`21.10.64`（已转正，第三方旧数据 21.10.41-beta 过时；FML 10.0.32）、1.21.11=`21.11.42`（已转正；FML 10.0.36）；
4. ✅（P1）ModMenu（Modrinth API）：1.21.9/1.21.10=`16.0.1`、1.21.11=`17.0.0`；
5. ⏳（P3）1.21.10 协议/数据版本号（预期与 1.21.9 相近）。

**风险**：
- **loom 升级连带**：新 loom 对本仓库四子项目结构（common 共享 loom、覆盖源集 filter、guiSystemTest natives classpath）的兼容性——P1 升级后需逐项回归既有构建逻辑，特别是 `GuiTestSupport` 依赖的 loom `minecraftNatives` classpath 配置在新 loom 下的名称与位置；
- **fabric 拆源集工作量**：若 §4.4 走方案 1，客户端类迁移 + `fabric.mod.json` entrypoints + split 配置，是 P1 最大的不确定项；
- **1.21.9 blur 行为漂移**：着色器重构后 ConfigScreen 双 blur 结构守卫（场景 3）的断言基线可能需按 1.21.9 实际行为调整——属守卫适配，不回退功能；
- **NeoForge beta 质量**：21.9/21.11 若终版仍 beta-only，按 G5 先例（21.6/21.7 beta 可用）接受，但实机回归加重覆盖；
- **网络盲区重演**：21.7 教训（离线全绿、启动即崩）——P2/P4 每版必须 sources 复核网络层 + 实机启动验证，不能只信编译。

## 8. 验证策略

### 8.1 离线测试资产全景（common/src/test 主源集 47 类，吸收后全量沿用）

| 层 | 测试 | 版本敏感性说明 |
|---|---|---|
| GUI 场景 | GuiScenarioTestMain 五场景：配置按钮坐标级点击、EditBox 真实键盘路径、双 blur 结构守卫、渲染方法引用守卫、文字颜色 alpha 完整性 | 场景 3/5 为**框架行为分支断言**设计（运行时探测「框架是否内置背景渲染」「drawString 是否 alpha==0 早退」再决定断言方向），1.21.9 着色器重构后自动落入对应分支；若行为再变，仅调守卫基线不回退功能 |
| Screen 级 | ConfigScreen / TodoScreen / AddProject / AddMember / ProjectSettings / ConfirmDeleteProject / TaskListWidget / ProjectListWidget / ScrollBar + ScreenDriver | 覆盖组替换实现后按当次 profile 编译产物生效，无需改测试 |
| 命令 | CommandSystem / CommandBootstrap×5 集成（含 H2）/ TaskPacketsPermission / CommandInputNormalizer | 版本无关 |
| H2/存储 | H2TcpAccess（TCP 路径）/ H2StorageBackendIntegration（嵌入式）/ SchemaInitializer / TaskQueryService / MaintenanceBackup / StorageAvailability / Diagnostic / LegacyMigration / StorageBackendSelection | 双路径离线覆盖齐全（规则 15/16） |
| 任务/客户端 | TaskManagerSubtask / TaskCompatibility / TodoHudRenderer / ClientTaskStorageHelper | TodoHudRenderer 测试随吸收版主源集生效 |
| 持久化 | PersistenceSafetyTestMain | 取 v1_21_6 组版本吸收（§5） |
| 守卫工具 | ClassFileMethodScanner / GuiTestSupport（natives 注入） | natives classpath 随 loom 升级需回归（§7） |

fake 类说明：FakeClientPlayer / FakeGameNarrator / FakeSoundManager 吸收 v1_21_6 组版本为主源集；FakeMinecraftClient / FakeClientConnection / FakePlayerInfo / RecordingClientOps 为 1.21.1 语义主源集版，若 1.21.9 vanilla 签名漂移导致编译失败，随 P2 直接更新（新分支最低支持 1.21.9，改主源集合法）。

### 8.2 新增测试需求（本分支补强）

1. **NeoForge 网络反射探测离线测试**（P2 落地）：neoforge 子项目新增测试，直调 `NeoForgeNetworkBridge.findFourArgPlayBidirectional()`，断言在 1.21.9+ 依赖类路径下返回非 null——离线验证反射探测确实命中目标 NeoForge 版本的四参方法，覆盖 21.7 缺陷④盲区中可离线的部分；三版本 profile 各自执行。启动校验的最后一段盲区仍靠实机冷启动兜底。
2. **1.21.11 通道 id 守卫**（P4 落地）：断言 v1_21_11 组 `NeoForgeDispatchPayload` 的通道 id 字符串仍为 `todolist:bridge`——Identifier 重命名只允许改类型名，不得改变字符串字面量，保住网络通道与既有数据引用的连续性。
3. 场景 3/5 在 P2 首跑时记录框架行为探测结果（背景渲染分支 / alpha 早退分支），与 1.21.9 官方渲染重构说明对照留档，作为后续 1.21.10/1.21.11 的基线参照。

### 8.3 profile 级执行

三版本各自执行全量链：`.\build-local-121x.bat <MC版本> build --offline`（含 `:common:check` 全部测试与五场景 + 三子项目 build + dist 聚合）。场景 4/5 的字节码扫描针对当次 profile 编译产物，覆盖组替换版本自动生效；`GuiScenarioTestMain` 无需按版本分支维护。

### 8.4 实机回归清单（P2 起每版执行）

- **启动**：fabric 端 + NeoForge 端各自冷启动（网络层注册，缺陷④回归）；
- **配置屏**：开合、配置项 label 显示（缺陷③）、按钮点击、输入框输入、blur 背景（缺陷①）；
- **HUD**：显示与内容（缺陷②）；
- **数据链路**：团队保存 → 重开待办 → 切回个人项目；**至少一版使用发布版 jar（dist 产物）验证 H2 TCP 激活路径**（规则 15/16 双路径要求）；
- **跨版本数据**：1.21.x 分支版本的存档在 1.21.9+ mod 下打开读取正常（存储格式理论版本无关，实机确认一次）；
- **1.21.11 追加**：同版本双端联机正常（通道 id 兼容最终验证）。

## 9. 提交规范

沿用仓库 Conventional Commits 约定。预期提交序列：
- `chore(port): 新建 1.21.9-1.21.11 分支基建`（P0 吸收+清组+矩阵重置）
- `build(matrix): 校准 1.21.9~1.21.11 版本矩阵`（P1，含 loom/Gradle 升级）
- `feat(port): 主源集适配 1.21.9`（P2）
- `feat(port): 1.21.10 搭车接入`（P3）
- `feat(port): 新增 v1_21_11 覆盖组适配 Identifier 重命名`（P4）
- 各阶段 docs/fix 提交按需。

git 提交由用户执行或经用户明确授权后执行。
