# Features（v1.4.1）

TodoList 是一个运行在 Minecraft 上的待办事项模组，支持单人游玩、本地局域网与多人服务器协作。本文主要面向普通玩家，介绍 1.4.1 版本里你现在可以直接用到的功能。

## 任务管理

- 在游戏内 GUI 中创建、编辑、删除任务，并可随时标记完成/未完成
- 支持子任务，可以把一个大任务继续拆成更细的小步骤
- 任务属性：标题、描述、优先级（低/中/高）、标签（逗号分隔）
- 搜索与筛选：按状态、优先级、文本（标题/描述）、标签实时过滤
- 未完成任务支持直接拖拽排序，便于快速整理优先级
- 父任务支持直接批量处理直属子任务，减少一个个点选的重复操作
- 已完成任务与未完成任务分段显示，可折叠，并支持在当前项目范围内一键清理已完成项
- 删除任务与清理已完成任务都带有显式确认，降低误操作风险
- GUI 详情编辑支持自动保存开关；开启后可在失焦、切换任务、切换项目、关闭界面前自动保存当前改动

## 项目与视图

- 支持个人项目和团队项目，单人整理或多人协作都能用
- 侧边栏可以搜索、切换、新建、编辑和删除项目
- 项目多的时候，可以配合搜索前缀和下拉提示更快找到目标项目
- 多人服务器里可以按“待分配”“已分配”“分配给我”几个视图查看团队任务
- 团队项目支持成员管理、加入申请和团队权限设置
- 星标项目会排在更前面，也可以作为 HUD 的展示来源
- 团队项目支持开启“全服任务模式”，适合更开放的共享协作玩法

## 多人协作与权限

- 多人模式下，个人任务会按玩家分别保存；团队任务则由服务器统一保存并同步给所有成员
- 团队视图里的改动需要点击保存；如果点取消或按 Esc 关闭，会放弃这次未保存修改并重新读取服务器内容
- “指派他人”弹窗会直接显示当前项目成员，离线成员也能继续被指派
- 父任务可以一键完成或取消完成它下面的直属子任务；在团队项目里，也能一键领取或指派还没分配的直属子任务
- 在“分配给我”视图里，父任务的批量完成、取消完成和放弃，只会影响你自己已经领取的直属子任务；已经完成的子任务也会继续显示，方便你再改回来
- 管理员可以管理团队任务、成员和关键命令
- 普通玩家可以领取、放弃并完成分配给自己的任务；“指派他人”只对管理员显示

## 命令系统

- 提供统一 `/todo` 命令入口，`/todolist` 也可以继续使用
- 可以通过命令快速新增、完成、删除、领取、放弃任务，也支持项目切换和 HUD 显示控制
- 管理者还能使用团队成员管理、H2 状态检查、备份、数据库重载、健康检查等命令

## HUD 展示与配置

- 右上角 HUD 会显示当前视图下的任务列表，支持展开/收起（H 键）和数量提示
- HUD 标题会跟着当前视图变化，方便你分清是在看个人任务还是团队任务
- 真单人世界里 HUD 会固定显示个人任务，并隐藏团队视图
- 内置 HUD 配置界面支持调整宽度、高度、显示条数、默认展开状态、空列表显示和拖拽位置，并可直接保存应用
- 如果安装了 Mod Menu，也可以从模组列表里打开 HUD 配置界面
- HUD 的颜色、统计和筛选逻辑会和主界面保持一致，日常查看更直观

## H2 存储与维护

- 现在模组统一使用 `H2` 存储，旧的 NBT 数据会在首次需要时自动迁移，正常情况下不用手动处理
- 你的任务、项目和项目状态都会继续正常保存
- 如果你是服主或管理者，可以使用 `/todo h2 status` 查看当前存储状态
- 需要排查问题时，也可以用 `/todo h2` 相关命令做备份、重载数据库和健康检查

## 持久化与多语言

- 任务、项目、玩家项目状态和配置文件都会用更稳妥的方式保存
- 如果主数据文件损坏，会尝试从最近一次备份自动恢复，并保留损坏文件方便排查
- 本地单人的个人任务会额外做同步保护，减少意外回滚或清空的情况
- 界面提供中文和英文语言包，会跟随游戏语言切换

## 审计日志

- 服务端会记录团队任务的重要操作，方便服主排查问题

