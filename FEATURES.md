# Features（v1.4.0）

TodoList 是一个运行在 Minecraft 上的待办事项模组，支持单人游玩、本地局域网与多人服务器协作。本文聚焦描述 1.4.0 已具备的能力（用户向）。

## 任务管理

- 在游戏内 GUI 中创建、编辑、删除任务，并可随时标记完成/未完成
- 任务属性：标题、描述、优先级（低/中/高）、标签（逗号分隔）
- 搜索与筛选：按状态、优先级、文本（标题/描述）、标签实时过滤
- 未完成任务支持直接拖拽排序，便于快速整理优先级
- 已完成任务与未完成任务分段显示，可折叠，并支持在当前项目范围内一键清理已完成项
- 删除任务与清理已完成任务都带有显式确认，降低误操作风险
- GUI 详情编辑支持自动保存开关；开启后可在失焦、切换任务、切换项目、关闭界面前自动保存当前改动

## 项目与视图

- 支持个人项目与团队项目两种维度
- 侧边栏项目列表：搜索、切换项目；支持新建、编辑、删除项目
- 项目搜索支持前缀与下拉提示，便于在项目较多时快速定位目标项目
- 团队任务视图（多人服务器）：待分配、已分配、分配给我
- 团队项目支持成员管理、加入申请与团队权限设置
- 星标项目会优先排序，也可作为 HUD 的项目来源
- 团队项目支持开启“全服任务模式”，适配更开放的共享协作场景

## 多人协作与权限

- 多人下个人任务按玩家独立保存；团队任务由服务器共享存储并同步给玩家
- 团队视图的改动需要点击保存提交；点击取消或按 Esc 关闭会丢弃本地未保存改动并从服务器刷新，减少冲突
- 服务器侧权限中心按角色、视图范围、完成状态与指派关系统一校验团队操作
- “指派他人”弹窗统一展示项目成员，离线成员也可继续被指派
- 角色差异：
  - 管理员可完整管理团队任务、项目成员与关键命令
  - 普通玩家可领取/放弃任务并完成分配给自己的任务；“指派他人”仅对管理员可见

## 命令系统

- 提供统一 `/todo` 命令入口，并保留 `/todolist` 作为别名
- 任务命令支持列表查询、分页、快速新增、项目内新增、完成、删除、指派、领取、放弃与批量清理（需二次确认）
- 项目命令支持列出、选择、星标、重命名、创建/删除项目，以及成员新增、移除、角色调整
- HUD 命令支持显隐切换
- 管理命令支持命令开放级别切换、H2 状态检查、在线备份、健康检查、TCP 重启、数据库重载与密码重置

## HUD 展示与配置

- 右上角 HUD 显示当前视图的任务列表，支持展开/收起（H 键）与数量提示
- HUD 标题展示当前视图（个人 / 团队-待分配 / 团队-已分配 / 团队-分配给我）
- 真单人世界中 HUD 默认视图固定为个人视图且隐藏团队视图
- 内置 HUD 配置界面：宽度、最大高度、待办/已完成显示条数（0–30）、默认展开、空列表是否显示、默认视图、拖拽预览设置位置，并可保存并应用
- 若安装 Mod Menu，可从模组入口打开 HUD 配置界面
- HUD 已同步优先级色块、隐藏数量语义与当前视图筛选逻辑；在 H2 模式下会优先使用数据库查询优化高频展示路径

## H2 存储与维护

- 底层存储支持在 `NBT` 与 `H2` 之间切换；`H2` 模式下可自动迁移旧 `.dat` 数据
- H2 覆盖任务、项目、玩家项目状态、查询优化、备份、健康检查、维护锁与 schema 升级链路
- H2 支持 TCP 外部访问，可按需开放本机或远程连接，并提供 `admin / readonly / readwrite` 三类账号
- 支持在线备份、数据库重载、TCP 重启、健康检查与密码重置等运维动作

## 持久化与多语言

- 任务、项目、玩家项目状态与配置文件以更安全的文件写盘机制保存（单人/多人均支持）
- 主文件损坏时会尝试从最近一次备份自动恢复，并保留损坏现场文件
- 本地单人个人任务会同步维护本地文件与玩家文件副本，减少意外回滚或清空现象
- 界面提供中文与英文语言包，可随游戏语言切换

## 审计日志

- 服务端会以统一 `[TEAM_OP]` 格式记录所有生效的团队操作，便于排查与审计

