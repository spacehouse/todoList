# Changelog

所有重大变更会记录在此文件中。

日期格式：`YYYY-MM-DD`

## [1.4.2] - 2026-09-11

### 新增功能

- **新增 Minecraft 1.21.9 / 1.21.10 / 1.21.11 版本支持**：覆盖 Fabric 与 NeoForge 双端。

### 优化与修复

- **移除进入世界的系统提示**：不再在进入世界后向管理员推送 H2 存储模式与管理命令提示；相关命令仍可通过 `/todolist h2 status`、`/todolist h2 backup`、`/todolist h2 reload-db`、`/todolist h2 health` 使用。
- **移除 H2 TCP 远程访问**：H2 数据库不再提供 TCP 远程连接能力，始终以嵌入式模式运行；原 TCP 配置与相关命令一并移除。
- **GUI 编辑自动保存默认开启**：GUI 编辑自动保存开关默认改为开启，无需手动配置即可在编辑后自动保存。

## [1.4.1] - 2026-08-16

### 新增功能

- **支持子任务**：现在一个任务下面可以继续添加子任务了，适合把大任务拆成更细的小步骤来管理。
- **父任务操作更方便**：点父任务时，可以一键完成或取消完成下面的直属子任务；在团队项目里，也可以一键领取或指派还没分配的直属子任务。

### 优化与修复

- **默认统一使用 H2 存储**：模组现在会直接使用 H2 存储模式，不再继续维护旧的 NBT 存储作为日常使用方案；如果你以前是旧 NBT 数据，首次进入后会自动迁移到 H2，一般不需要手动处理。
- **进入世界后的提示更清楚**：现在会提示常用的 H2 管理命令，比如查看状态、备份、重载数据库和健康检查，服主排查问题会更方便。

## [1.4.0] - 2026-06-17

### 新增功能

- **H2 存储后端正式落地**：新增 `storageBackend=h2` 关系型存储模式，覆盖任务、项目、玩家项目状态、旧 `.dat` 自动迁移，以及基于 SQL 的 GUI/HUD 查询优化。
- **H2 TCP 外部访问与维护命令**：新增 H2 TCP 配置、三级账号（`admin / readonly / readwrite`）、在线备份、健康检查、数据库重载、TCP 重启与密码重置能力，方便在本机或局域网中接入外部工具。
- **命令系统扩展成型**：`/todo` 命令从基础任务操作扩展到项目管理、成员管理、HUD 控制、加入审批、命令开放级别切换以及 H2 运维入口，并保留 `/todolist` 别名。
- **已完成任务一键清理**：在 GUI 已完成分组中新增当前项目范围的一键清理入口，并加入二次确认流程。
- **GUI 编辑自动保存开关**：配置页新增 GUI 自动保存开关，支持详情编辑在失焦、切换任务、切换项目和关闭界面前自动保存。

### 优化与修复

- **HUD 与 GUI 查询链路优化**：在 H2 模式下将项目计数、HUD 列表与 GUI 高阶筛选逐步切到数据库查询，减少高频全量扫描。
- **H2 生命周期稳定性修复**：修复局域网发布、退出、回到未发布单人等场景下失效连接被复用的问题，降低“当前存储不可用”误报。
- **H2 写入可用性修复**：修复 H2 锁等待被误判为存储不可用、个人任务保存性能回退，以及团队保存后切回个人项目卡顿等问题。
- **HUD 与输入体验修复**：修复 HUD 快速切换闪烁，优化展开态统计文案，并统一描述/标签输入提示的显示语义。
- **任务交互稳定性修复**：修复异步保存与团队同步竞争、团队任务快速操作闪屏，以及任务同步后的 GUI/HUD 刷新滞后问题。
- **配置说明补强**：补充 `storageBackend` 与 `h2BackupOnStart` 的多语言说明注释，降低配置误用成本。

### 文档与运维

- **H2 使用文档补齐**：新增底层数据模式切换说明和外部工具连接指南，补齐 `NBT/H2` 切换、TCP 访问、账号使用与常见排错文档。

## [1.3.0] - 2026-04-26

### 新增功能

- **待办主界面重构**：重组主界面布局、项目侧栏、任务列表与详情抽屉，补齐响应式空间分配与更清晰的视图切换流程。
- **未完成任务拖拽排序**：支持直接拖拽调整未完成任务顺序，便于在游戏内快速整理个人与团队待办优先级。
- **任务分段列表与删除确认**：任务列表支持未完成/已完成分段展示与折叠基线，并新增删除确认流程，降低误删风险。
- **团队项目搜索与成员交互增强**：新增项目搜索前缀与下拉提示，统一成员选择/指派弹窗布局，提升多人协作下的筛选与操作效率。
- **团队项目全服任务模式**：支持在团队项目中开启“全服任务模式”，让团队任务在协作场景下拥有更统一的领取与处理语义。

### 优化与修复

- **HUD 与视图联动增强**：同步 HUD 优先级色块、隐藏数量语义与团队视图展示逻辑，让 HUD 与主界面视图表现更一致。
- **团队交互反馈优化**：优化任务领取、放弃、指派与团队全部视图中的反馈表现，减少操作成功但界面未及时响应的割裂感。
- **任务完成状态即时持久化**：修复任务切换完成状态后未实时持久化的问题，减少关闭界面或刷新后状态回退的概率。
- **拖拽排序判定修复**：修复拖拽排序有时需要二次点击才生效的问题，改善拖拽操作的连续性。
- **单人个人任务回灌修复**：修复本地单人模式下个人任务文件与玩家文件双副本漂移导致的偶发回滚/清空问题。

### 稳定性与测试

- **安全写盘与自动恢复**：任务、项目、玩家项目状态与配置文件统一切换到临时文件写入、备份恢复与损坏现场保留机制，增强崩溃/断电场景下的数据可靠性。
- **项目重载保护**：项目数据重载改为两阶段加载，避免读取失败时先清空内存态再写回造成二次损伤。
- **持久化回归测试补充**：新增持久化安全回归测试与单人个人任务双副本同步测试，提升发布前回归验证覆盖。

## [1.2.2] - 2026-03-29

### 优化与修复

- **HUD 状态同步优化**：增强 HUD 显隐状态同步逻辑，改善客户端与服务端之间的状态一致性。
- **局域网团队同步优化**：优化局域网发布场景下的团队项目与团队任务同步链路，提升多人协作稳定性。
- **个人任务本地隔离**：重构个人任务存储逻辑，改善本地单人模式下的数据隔离，减少不同运行环境之间的任务串扰。
- **跨项目保存修复**：修复跨项目切换后的保存异常，避免错误覆盖或遗漏保存内容。
- **LAN 个人任务恢复修复**：修复局域网场景下个人任务恢复不正确的问题，提升本地/联机切换后的数据稳定性。
- **NeoForge 同步链路修复**：补齐 NeoForge 端 HUD 同步与 LAN 状态恢复逻辑，改善多平台行为一致性。
- **指派弹窗成员来源修复**：修复“指派他人”弹窗错误显示在线玩家列表的问题，改为展示当前团队项目成员列表，离线成员也可正确显示与指派。
- **指派弹窗布局修复**：优化“指派他人”弹窗的自适应布局，在较小窗口或较大界面缩放下，成员列表会自动调整高度，避免取消按钮被挤出界面。

### 测试与稳定性

- **离线自测支持增强**：补充 GUI 组件、命令系统和提交流程的离线自测能力，提升回归验证效率。
- **集成测试完善**：新增并拆分命令系统相关测试入口，细化项目、任务与加入审批流程的覆盖范围。
- **1.21.1 测试适配**：适配 `1.21.1` 分支的离线测试桩与 GUI 提交流程，为后续维护提供更稳定的验证基础。

## \[1.2.1] - 2026-03-21

### 优化与修复

- **团队项目可用性修复**：修正了本地局域网世界中的团队项目可用性判断逻辑，确保本地局域网世界下项目状态及权限的正确显示和处理。

## \[1.2.0] - 2026-03-19

### 新增功能

- **平台支持升级**：正式支持 Minecraft `1.21.1` 的 Fabric / Forge / NeoForge 三端客户端与服务端发布和运行。

### 优化与修复

- **Forge 网络加载修复**：修复了 `Failed to create Forge channel` 导致的初始化失败，改进通道创建兼容与错误诊断日志。
- **Fabric GUI 可读性修复**：统一任务与项目相关页面背景渲染为清晰实底，修复背景模糊导致文字难以辨认的问题。
- **NeoForge 按键绑定修复**：补齐 `H/J/K` 在 NeoForge 下的按键注册与触发链路，控制设置可见且可正常触发 HUD/GUI 行为。
- **发布流程增强**：发布工作流新增 NeoForge 发布，Fabric/Forge 适配版本更新为 `1.21.1`，GitHub Release 标题统一为 `TodoList-vX.Y.Z-release`，并修复按 tag 从 `CHANGELOG.md` 提取发布说明。

## \[1.1.2] - 2026-03-17

### 优化与修复

- **按键绑定修复（Forge）**：修复了 Forge 端按键未正确注册到 Mod 事件总线的问题，`K/H/J` 现在会在控制设置中正常显示并可自定义。
- **配置界面增强**：在 HUD 配置中新增“HUD显示”开关，可直接切换 HUD 显示状态。
- **交互一致性**：统一按键触发逻辑为 `consumeClick()`，并保持按住按键时可重复触发的行为一致性。
- **保存语义一致性（Forge/Fabric）**：统一个人任务的保存行为为“仅点击保存按钮才落盘”；关闭界面会丢弃未保存改动，修复了此前 Forge 端个人任务看起来会自动保存、与 Fabric 不一致的问题。
- **HUD 性能优化（渲染层）**：重构 HUD 渲染数据管线，加入模型节流与行排版缓存，减少每帧对象分配与文本排版计算开销。
- **Forge 个人任务同步保护**：当本地存在个人任务未保存改动时，Forge 客户端会跳过个人任务同步包的本地落盘，避免未保存改动被覆盖或看起来“自动生效”。

## \[1.1.1] - 2026-03-09

### 优化与修复

- **性能优化**：显著优化了 HUD 渲染性能，通过减少对象创建与移除冗余渲染调用，解决了开启 HUD 后 FPS 大幅下降的问题（尤其在 Forge 端效果明显）。
- **Fabric 修复**：优化了 HUD 渲染性能。

## \[1.1.0] - 2026-03-08

### 新增功能

- **多平台支持**：正式引入 Forge 平台支持，实现 Fabric/Forge 双平台兼容。
- **服务端同步增强**：实现了客户端激活项目状态向服务端的实时同步，优化了多人游戏下的数据隔离与状态一致性。
- **任务命令扩展**：增强了服务端任务管理命令，支持更细粒度的任务操作。

### 优化与修复

- **GUI 交互改进**：修复了任务列表的滚动与边界处理逻辑，优化了配置界面的布局显示。
- **网络稳定性**：修复了 Forge 客户端在连接状态切换时可能导致的数据同步异常。
- **代码质量**：重构了网络包注册架构，修复了大量中文注释乱码，提升了代码可维护性。

## \[1.0.0] - 2026-02-21

### 能力概览

- **任务管理**：在游戏内 GUI 中创建/编辑/删除/完成任务；支持优先级（低/中/高）、标签、搜索与筛选（状态/优先级/文本/标签）。
- **项目维度**：支持个人项目与团队项目；侧边栏项目列表/搜索与项目切换；支持新建、编辑、删除项目，并提供团队项目成员管理与加入申请。
- **HUD 展示与配置**：右上角 HUD 显示当前视图任务列表，支持展开/收起与数量提示；单人世界 HUD 固定为个人视图并隐藏团队视图；提供 HUD 配置界面（尺寸、显示条数、默认视图、空列表显示、位置拖拽预览等）。
- **多人协作**：多人下个人任务按玩家独立保存；团队任务在服务器侧共享并同步给玩家；团队视图改动需保存提交，取消/关闭会丢弃本地改动并从服务器刷新，减少冲突。
- **指派与执行流**：团队任务支持领取/放弃/指派与“分配给我”等视图；离线玩家的指派人名称可回退显示，保证可读性。
- **权限与审计**：服务端权限中心基于角色、视图范围、完成状态与指派关系判定团队操作；所有生效的团队操作写入统一操作日志（`[TEAM_OP]`）。
- **持久化与多语言**：任务与项目数据以文件持久化（单人/多人均支持）；界面与文档提供中英文版本。
